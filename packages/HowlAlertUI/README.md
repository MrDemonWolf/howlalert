# HowlAlertUI

Shared SwiftUI design system — design tokens + 20 components, imported by both
Xcode projects (`apps/macos`, `apps/mobile`) via a local Swift Package
reference.

- Platforms: **macOS 26 / iOS 26 / watchOS 26** only.
- Excluded from the Bun workspaces (Swift, not JS).

See HAA-112.
