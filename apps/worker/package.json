{
  "name": "@howlalert/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'worker lint noop'",
    "test": "echo 'worker tests land in phase 1'"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "@howlalert/config": "workspace:*",
    "@howlalert/shared-types": "workspace:*",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
